package test;

public class ExcelRead {
    public static void main(String[] args) throws Exception {
        //  File file=new File();
        //System.out.println(file.exists());
        //LOAD THE FILE INTO HAVA MEMORY USING FILEINPUTSTREAM
        //FileInputStream fileInputStream=new FileInputStream(file);
        //Load the excel workbook into the java class
        //   XSSFWorkbook workbook=new XSSFWorkbook(fileInputStream);
        //workbook>sheet>row>cell

        //getting the sheet from the workbook

        // XSSFSheet sheet=workbook.getSheet("Emplyees");
        // System.out.println(sheet.getRow(2).getCell(1));
        // int usedRows=sheet.getPhysicalNumberOfRows();
        //System.out.println(usedRows);

        //TODO: 1-create a logic that prints out neena's name dinamically

        //for(int rowNum=0;rowNum<usedRows;rowNum++){
        //if(sheet.getRow(rowNum).getCell(cellNum:0).equals("Neena")){
        //sout("Neena's name: "+ sheet.getRow(rowNum).getCell(cellNum:0));

        //TODO:2-Create a logic that prints out Adam's last name dynamically
       // for(int rowNum=0;rowNum<usedRows;rowNum++){
        //if(getRow(rowNum).getCell(cellNum:0).toString().equals("Adam")){
        //sout("Adam's last name: "+getRow(rowNum).getCell(cellNum:1));


        //TODO: 3- Print out steven's job id dinamically
        //for(int rowNum=0;rowNum<usedRows;rowNum++){
        //if(sheet.getRow(rowNum).getCell(cellNum:1).toString().equals("King"));
        //sout("Steven's job id: "+sheet.getRow(rowNum).getCell(cellNum:02));
    }


}
